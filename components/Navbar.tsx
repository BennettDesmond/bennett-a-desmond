import {
    Text,
} from '@chakra-ui/react'

interface NavbarProps {
}

export default function Navbar(props: NavbarProps) {
    return (
        <>
            <Text>
                Navbar Section
            </Text>
        </>
    )
}